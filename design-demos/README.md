# 电♥百展示页 · 三方向初稿

这里是设计阶段留下的记录。站点标题已经定为「电♥百」，副标题是「电子科技大学百合 ACG 交流群」。

先看 `index.html`，再打开各方向的完整页面。所有 HTML 都是自包含的，不需要联网，也不需要安装依赖，直接双击就能打开。原来的 `/design-preview/` 路由已经删除，不能再通过开发服务器查看这些稿子。

| 文件 | 方向 |
| --- | --- |
| `star-atlas.html` | A · 同好星图 |
| `creative-journal.html` | B · 社团创作刊 |
| `flower-letters.html` | C · 花间来信（已选定） |

- **选择记录**：用户选定了 C「花间来信」，见 `direction-approved.md`。
- **旧文案**：稿子里的「百合社」名称、活动、作品和介绍文案都是设计示例，不代表真实记录，图片也都是占位。
- **现在的文案**：正式的标题、文案和板块以 `../docs/claude_recommendation.md` 为准。这个站点只做展示，不放任何入群方式。
- **正式首页**：`src/pages/index.astro` 目前还是「正在施工」的占位页，尚未按 C 方向实现。
- **其他文件**：设计依据见 `design-spec.md`、`brand-spec.md`，其中的名称和文案以 `docs/` 下的文档为准。浏览器截图在 `screenshots/`。
